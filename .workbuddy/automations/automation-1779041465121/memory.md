# AI Researcher - 每日数据抓取 执行记录

## 2026-06-23 17:55 执行

**状态**：⚠️ API 启动但抓取未推进

**结果**：
- POST /api/data/fetch-all → HTTP 200，started_at=17:55:48，mode=incremental，count=99
- 等待 5+ 分钟后 fetch-status 仍为 `progress: 0/99, processed: 0, success: 0, worker_active: true`
- fetch-step-status 中今天 17:00 后无任何股票更新记录，最近一批仍是 6-20 ~ 6-23 早间
- 后端进程健康（PID 52194, python app.py, /api/health → ok, db_initialized=true）

**根因**：
- 与 2026-06-22 完全相同的失败模式：API 返回 200 但后台 worker 实际未推进
- 已知缺陷（见 6-23 工作日志 15:25 条目）：`fetch-all` 在并发下不稳定，DataFetcher 缺乏 HTTP timeout，会因 DB 锁/僵尸进程导致 worker 挂死
- 错误信息字段为空，无法定位具体股票级失败

**建议改进**：
- 在抓取脚本里增加 progress 进度检查与超时机制（processed==0 持续 N 分钟应自动判定失败并放弃）
- 改用更可靠的 `/api/stocks/{id}/fetch` 单股串行抓取接口
- 为 `DataFetcher` 加 HTTP timeout 参数

---

## 2026-06-08 18:00 执行

**状态**：✅ 成功

**结果**：
- 57 只活跃股票全部完成数据更新（57/57）
- 日线数据已更新至 2026-06-08，57/57 只股票均有今日数据
- 数据库总量：日线 83,374 条、财报 4,714 条、财务指标 4,006 条
- 抓取由后端调度器自动触发，开始 17:57，完成约 18:15（约18分钟）
- 自动化轮询到已有任务运行中（7/57），后续耗时主要在个别股票 API 响应慢

---

## 2026-06-02 18:00 执行

**状态**：✅ 成功

**结果**：
- 57 只活跃股票全部完成数据更新（57/57）
- 日线数据已更新至 2026-06-02，57/57 只股票均有今日数据
- 数据库总量：日线 83,146 条、财报 4,714 条、估值快照 387 条、因子评分 446 条
- 抓取开始 17:58，完成约 18:00（约2分钟，因首次轮询即已完成）

---

## 2026-05-28 22:02 执行

**状态**：✅ 成功

**结果**：
- 54 只活跃股票全部完成数据更新
- 进度 54/54，耗时约 30 分钟
- 抓取开始 22:02，结束约 22:32
- 包含日线数据、财务报告、财务指标、估值快照、因子评分

**备注**：
- API 手动触发时已有上一个调度任务在运行（13/54），触发后重置并重新开始
- 抓取速率约 1 只/30秒，部分慢速 API 调用拉长了总耗时

---

## 2026-05-22 18:00 执行

**状态**：✅ 成功

**结果**：
- 31 只活跃股票全部完成数据更新
- 日线数据：29,825 条（最新到 2026-05-22）
- 财务报告：2,722 条
- 财务指标：2,709 条（PE/PB/ROE/ROA 等）
- 估值快照：53 条（16 只为首次抓取，仅 1 条）
- 因子评分：37 条

**备注**：
- 调度器（app.py scheduler_loop）和 API 手动触发均执行了抓取，存在双线程竞争 _fetch_all_status 导致进度显示异常跳动
- 但因调度器独立循环不受影响，数据最终全部抓取成功
- 抓取耗时约 25 分钟（单只股票 1-2 分钟不等，部分 API 响应慢）

---

## 2026-06-22 18:00 执行

**状态**：⚠️ 抓取启动但未完成

**结果**：
- POST /api/data/fetch-all 返回 200，启动增量抓取 99 只股票
- 后端进程在抓取开始后重启（SpawnProcess-41 → SpawnProcess-42+），导致后台 worker 被终止
- fetch-status 显示 finished=true, progress=0/0, total=0 — 实际无数据被处理
- fetch-step-status 保留的是历史数据（最近更新日期 2026-06-20/06-22 早间），本次抓取未产生新数据
- 原因推测：uvicorn auto-reload 或进程重启导致后台异步任务丢失

**备注**：
- 后端服务正常运行（HTTP 200），但后台 worker 未能持久运行完成抓取
- 下次执行需注意：如果后端刚重启，fetch-all 的异步 worker 可能因进程再次重启而丢失
