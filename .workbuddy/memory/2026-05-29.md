# 2026-05-29 工作日志

## 全局政策扫描（自动化任务）
- LLM 调用成功（DeepSeek V4-Pro, 13行业覆盖）
- 数据库写入失败：旧后端进程 PID 2885 (Python 3.14) 持有 SQLite 写锁，沙箱无法杀进程
- 修复了 `policy_global.py` 和 `api/policy.py` 的数据库锁冲突问题（改用全局连接 + write_lock）
- 修复了 `launch.sh` 使用系统 Python 而非 managed Python
- 新增 `write_policy_scan.py` 独立写入脚本
- 用户需手动 kill 2885 后重启

## 资金面分析全量修复 (54/54 成功)
- **根因**: `api/capital.py` 的 `_run()` 用裸 `sqlite3.connect()`（无 `row_factory`），传给 `resolve_calc_date()` 时 `row["calc_date"]` 访问 tuple 导致 "tuple indices must be integers or slices, not str"
- **修复**: 改用 `comprehensive_store._connect()` 创建连接（自带 `row_factory = sqlite3.Row`）
- **性能优化**: `compute_all_capital()` 只调一次，结果缓存到 `scores_map` 字典中分发，避免每只股票重算（从 54×54=2916 次降到 54 次）
- **DB_PATH**: 实际位于 `/Users/harrywang/WorkBuddy/2026-05-18-task-7/ai-fundamental-researcher/data/afr.db`（不是 backend/data/ 下）
- **状态查询端点**: `GET /api/stocks/capital/analyze-status`（不是 analyze-all/status）

## 每日政策面分析（自动化任务 14:10）
- 调用 `POST /api/stocks/policy/analyze-all`，54只股票全部分析完成
- 平均分 57.6，最高82.4(京东方A)，最低32.6(中国卫通)
- 信号分布：强利好1只、利好3只、偏利好17只、中性25只、偏利空7只、利空1只
- LLM 启用率 0/54（全部关键词评分）
- 报告文件: data/policy_report_2026-05-29.md

## 每日新闻面分析（自动化任务 12:57）
- 调用 `POST /api/stocks/news/analyze-all`，54只股票，50只有分数
- 全局新闻面均分: 54.9（较5/26 59.2 下降 4.3 分）
- TOP5: 铖昌科技(95) > 天孚通信(92) = 新易盛(92) > 京东方Ａ(90) > 美的集团(72)
- BOTTOM5: 中航沈飞(30) < 航发科技(25) = 柯力传感(25) < 再升科技(15) < 中国卫通(10)
- 结果导出至 data/news_analysis_20260529.json
