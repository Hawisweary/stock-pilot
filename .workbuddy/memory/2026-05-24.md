# 2026-05-24 工作记录

## 每日行情抓取自动化执行
- 15:30 触发 fetch-all，count=39
- daemon 线程仅处理 4 只后崩溃，fetch-status 自动重置为 finished
- stock_daily_quotes 无新数据写入，最新数据仍停在 2026-05-22
- 与 5月23日问题一致：daemon 线程不稳定 + 数据未写入 stock_daily_quotes
- 5月23日（周五）交易日数据缺失，需修复后补抓

## 每日资金面分析自动化执行
- 16:15 调用 POST /api/stocks/capital/analyze-all，39只全量完成，0错误，耗时21秒
- 数据日期 2026-05-23（周日非交易日，数据源未更新，与昨日一致）
- 综合评分：平均47.1，最高64.6（京东方A 000725），最低34.6（001270）
- 分布：偏多4 / 中性偏多15 / 中性偏空2 / 偏空18
- 产出：data/capital_dashboard.html
- 发现 DB 连接 bug：capital.py 中 `DB = "DB_PATH"` 应为 `DB = DB_PATH`（待修复）

## 每日新闻面分析自动化执行
- 16:10 调用 POST /api/stocks/news/analyze-all，API 返回 started 后后台异步完成
- 5月24日为周日，无新新闻产生，最新数据仍为 5/23 08:10
- 39只股票 / 392条新闻全部完成情感分析（分析率 100%）
- 全局新闻面均分: 55.5
- TOP5: 铖昌科技(84.0) > 拓斯达(83.4) > 京东方A(81.5) > 胜宏科技(77.1) > 双环传动(72.0)
- BOTTOM5: 中国卫通(21.0) < 三安光电(24.5) < 中国卫星(32.8) < 中航沈飞(33.0) < 中航西飞(38.0)

## 每日情绪面分析自动化执行
- 16:20 触发，API 返回 started，但后台线程因 DB = "DB_PATH" bug 写入错误文件
- 修复 sentiment.py 中 DB 字符串字面量 bug，改为引用 config.DB_PATH
- 同步执行 compute_sentiment_score 完成39只全量分析，0错误
- 平均评分44.9，最高: 招商银行(55.0)，最低: 京东方A(31.5)
- 情绪分布: 中性29只(74.4%) / 偏亢奋10只(25.6%)
- 删除了错误创建的 backend/DB_PATH 文件
- 报告: data/sentiment_report_2026-05-24.md

## 每日政策面全局扫描自动化执行
- 16:30 调用 POST /api/stocks/policy/global-scan，HTTP 200，耗时28.9s
- LLM: DeepSeek V4-Pro，1次调用覆盖16个行业，更新39只股票
- 高分: 电池(90)、汽车(88)、通信设备(85)、计算机设备(82)
- 低分: 建筑装饰(50)、钢铁(55)、特种化工(55)
- 报告: data/policy_scan_2026-05-24.html

## 系统修复（DB="DB_PATH" 批量修复）
- 发现 api/*.py 中多处 `DB = "DB_PATH"` 字符串字面量 bug（应为 `DB_PATH` 变量）
- 涉及文件: policy.py, scores.py, advanced.py, signals.py, capital.py, stocks.py, institution.py, technical.py, dashboard.py, backtest.py, report.py, premium.py 共 28 处
- 全部批量修复，服务重启后验证通过
- config.py 补充 AI_MODEL/AI_PROVIDER/DEEPSEEK_BASE_URL/OPENAI_API_KEY/OPENAI_BASE_URL/CLAUDE_API_KEY 变量

## 每日综合评分刷新自动化执行
- 16:35 触发，端点 /api/scores/batch 不存在(404)，实际使用 /api/scores/recalculate?benchmark=industry
- 遇到500错误：factor_engine.py 导入 FACTOR_BENCHMARK_DEFAULT 失败，config.py 缺少该常量
- 修复 config.py 添加 FACTOR_BENCHMARK_DEFAULT = "industry"，重启后端后成功
- 因子评分39只 OK + 综合评分39只 OK
- 平均分56.4，最高78.9(600862)，最低27.4(603601)
- 分布: >=70分6只 | 60-70分9只 | 40-60分20只 | <40分4只
